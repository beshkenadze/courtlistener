// sdk/courtlistener-api/src/example.test.ts
import { describe, it, expect } from 'vitest';

describe('SDK Setup', () => {
  it('should run a basic test', () => {
    expect(1 + 1).toBe(2);
  });
});
